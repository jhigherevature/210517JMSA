package functionalprogramming;

/*
 * Lambdas act as an implementation for a functional interface
 * during runtime
 */
@FunctionalInterface
public interface TakeInputInterface {
	public void printInput(int a, int b);
}
