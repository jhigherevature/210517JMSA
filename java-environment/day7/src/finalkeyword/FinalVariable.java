package finalkeyword;

public class FinalVariable {
	/*
	 * Variables using the final keyword cannot be
	 * changed once they are initialized (and you must
	 * provide an initialization for them).
	 * 
	 * You can initialize these variables within a constructor
	 * however
	 */
	public final int id = 123;
	
//	public FinalVariable(int id) {
//		this.id = id;
//	}
}
